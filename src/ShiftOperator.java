public class ShiftOperator {
//    Shift operators are used to shift the binary bits for a specific number of times on either left or right based on the users requests and the results are in a new decimal value.
    public static void main(String[] args) {
        int x = 10;
        System.out.println(x << 2);
        System.out.println(x >> 2);
    }
}
